"""Single source of truth for the running editor's version.

Bumped automatically by ``scripts/release.py`` before tagging, so the
constant always matches the latest tag on ``main``.
"""

__version__ = "0.6.0"
